---
const { title = "yacksons.world" } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <style>
      :root { color-scheme: dark; }
      body { margin: 0; background: #000; color: #fff; font-family: system-ui, sans-serif; }
      a { color: inherit; }
      .wrap { max-width: 900px; margin: 0 auto; padding: 20px; }
      header { display: flex; align-items: center; justify-content: space-between; gap: 16px; }
      nav a { margin-right: 14px; text-decoration: none; }
      nav a:hover { text-decoration: underline; }
      .main { display: grid; grid-template-columns: 1fr 220px; gap: 24px; margin-top: 24px; }
      .side a { display: block; margin-bottom: 16px; text-decoration: none; }
      .side img { width: 100%; height: auto; image-rendering: pixelated; }
      .logo { width: 64px; height: 64px; image-rendering: pixelated; }
      @media (max-width: 800px) {
        .main { grid-template-columns: 1fr; }
      }
    </style>
  </head>
  <body>
    <div class="wrap">
      <header>
        <a href="/" aria-label="Home">
          <img class="logo" src="/logo.gif" alt="" />
        </a>
        <nav>
          <a href="/blog/">Blog</a>
          <a href="/category/whatever/">Category</a>
          <a href="/about/">About</a>
        </nav>
      </header>

      <div class="main">
        <main>
          <slot />
        </main>

        <aside class="side">
          <a href="https://example.com" target="_blank" rel="noopener">
            <img src="/play-my-game.gif" alt="Play my game" />
          </a>
          <a href="https://example.com" target="_blank" rel="noopener">
            <img src="/listen-to-my-music.gif" alt="Listen to my music" />
          </a>
        </aside>
      </div>
    </div>
  </body>
</html>
